"use strict";

import { useMemo } from "react";
import { useStack } from "../../../hooks/navigation/use-stack";
import { KeysProvider } from "../../../providers/screen/keys.provider";
import { ScreenStylesProvider } from "../../../providers/screen/styles.provider";
import { getActiveFloatOverlay } from "../helpers/get-active-overlay";
import { OverlayHost } from "./overlay-host";

/**
 * Float overlay component that renders above all screens.
 * Gets routes and descriptors from stack context.
 */
import { jsx as _jsx } from "react/jsx-runtime";
export function FloatOverlay() {
  const {
    scenes,
    focusedIndex,
    flags
  } = useStack();
  const activeOverlay = useMemo(() => getActiveFloatOverlay(scenes, focusedIndex, flags.TRANSITIONS_ALWAYS_ON), [scenes, focusedIndex, flags.TRANSITIONS_ALWAYS_ON]);
  if (!activeOverlay) {
    return null;
  }
  const {
    scene,
    overlayIndex
  } = activeOverlay;
  const previous = scenes[overlayIndex - 1]?.descriptor;
  const current = scene.descriptor;
  const next = scenes[overlayIndex + 1]?.descriptor;
  return /*#__PURE__*/_jsx(KeysProvider, {
    current: current,
    previous: previous,
    next: next,
    children: /*#__PURE__*/_jsx(ScreenStylesProvider, {
      children: /*#__PURE__*/_jsx(OverlayHost, {
        scene: scene
      })
    })
  });
}
//# sourceMappingURL=float-overlay.js.map