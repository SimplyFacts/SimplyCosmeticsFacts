import { PersistentStorage } from '../types';
export declare class AsyncStorageWrapper implements PersistentStorage<string | null> {
    protected storage: AsyncStorageInterface;
    constructor(storage: AsyncStorageInterface);
    getItem(key: string): Promise<string | null>;
    removeItem(key: string): Promise<void>;
    setItem(key: string, value: string | null): Promise<void>;
}
interface AsyncStorageInterface {
    getItem(key: string): Promise<string | null>;
    setItem(key: string, value: string | null): Promise<void>;
    removeItem(key: string): Promise<void>;
}
export {};
